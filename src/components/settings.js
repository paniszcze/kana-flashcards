import '../styles/settings.css';

export default function Settings() {
  return (
    <form className="settings">
      <label>
        <span>...</span>
        <input type="text" />
      </label>
      <button>Save</button>
    </form>
  )
}